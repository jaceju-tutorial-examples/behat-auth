default:
    extensions:
        Laracasts\Behat:
            # env_path: .env.behat
        Goez\BehatLaravelExtension:
        Behat\MinkExtension:
            default_session: laravel
            laravel: ~
